klone-training deployed successfully!

{{- if .Values.klone.enabled }}

=== Static klone cluster ===
Terminal (web kubectl):
  kubectl get ingress -n {{ .Values.klone.namespace }} klone-terminal -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'

  Access: https://{{ .Values.klone.ingress.host }}.<tailnet>.ts.net
{{- end }}

{{- if .Values.klone.enabled }}

=== klone cluster manager ===
Health check:
  kubectl get ingress -n {{ .Values.klone.namespace }} klone -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'

  Access: https://{{ .Values.klone.ingress.host }}.<tailnet>.ts.net/healthz

Cluster manager UI:
  https://{{ .Values.klone.ingress.host }}.<tailnet>.ts.net
{{- end }}

Wait for all pods to be Ready:
  kubectl get pods -n {{ .Values.klone.namespace }}
  kubectl get pods -n {{ .Values.klone.namespace }}
